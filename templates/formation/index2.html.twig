{% extends '/dashboard/base2.html.twig' %}

{% block title %}Formations{% endblock %}

{% block content %}
<div class="container" style="margin-top: 7%;">
    <li class="breadcrumb-item"><a href="{{ path('app_dashboard') }}"></a></li>
    <h1 class="text-center mb-5" style="font-weight: 700; color: #2c3e50; text-shadow: 1px 1px 3px rgba(0,0,0,0.1);">Liste des Formations</h1>
    
    <!-- Affichage des messages flash -->
    {% for label, messages in app.flashes %}
        {% for message in messages %}
            <div class="alert alert-{{ label == 'success' ? 'success' : (label == 'error' ? 'danger' : 'warning') }} text-center fw-semibold" role="alert">
                {{ message }}
            </div>
        {% endfor %}
    {% endfor %}

    <!-- Barre de recherche -->
    <div class="mb-5 d-flex justify-content-center align-items-center flex-wrap">
        <form method="get" action="{{ path('app_formation_search') }}" class="d-flex w-75" style="max-width: 700px;">
            <div class="input-group shadow-sm w-100">
                <input type="text" name="search" class="form-control py-2 border-0" 
                       placeholder="Rechercher une formation..." 
                       value="{{ app.request.get('search') }}"
                       style="border-radius: 30px 0 0 30px;">
                <button class="btn btn-primary" type="submit" 
                        style="background-color:#4771cb; border-radius: 0 30px 30px 0; padding-left: 25px; padding-right: 25px;">
                    <i class="fas fa-search me-2"></i>Rechercher
                </button>
            </div>
        </form>

        <a href="{{ path('app_favori_list') }}" class="ms-3">
            <button class="btn" type="button"
                    style="background-color: #ff4757; color: white; border-radius: 30px; padding-left: 20px; padding-right: 25px; width: 200px;">
                <i class="fas fa-heart"></i> Mes favoris
            </button>
        </a>
    </div>

    <div class="filters mb-4 text-center">
        <form method="get" action="{{ path('app_formation_filter2') }}" class="d-flex justify-content-center flex-wrap gap-3">
            <select name="certifie" class="form-select w-auto">
                <option value="">Avec ou sans certificat</option>
                <option value="oui" {% if app.request.get('certifie') == 'oui' %}selected{% endif %}>Avec certificat</option>
                <option value="non" {% if app.request.get('certifie') == 'non' %}selected{% endif %}>Sans certificat</option>
            </select>

            <select name="type" class="form-select w-auto">
                <option value="">Type</option>
                <option value="présentiel" {% if app.request.get('type') == 'présentiel' %}selected{% endif %}>Présentiel</option>
                <option value="distanciel" {% if app.request.get('type') == 'distanciel' %}selected{% endif %}>Distanciel</option>
            </select>

            <select name="langue" class="form-select w-auto">
                <option value="">Langue</option>
                <option value="français" {% if app.request.get('langue') == 'français' %}selected{% endif %}>Français</option>
                <option value="anglais" {% if app.request.get('langue') == 'anglais' %}selected{% endif %}>Anglais</option>
            </select>

            <button class="btn btn-primary" type="submit" style="background-color:#4771cb; border: none;width: 70px;">Filtrer</button>
        </form>
    </div>
    
    {% if formations is not empty %}
        <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
            {% for formation in formations %}
                <div class="col">
                    <div class="card h-100 shadow-sm border-0" 
                         style="border-radius: 15px; transition: all 0.3s ease; overflow: hidden;">
                        
                        <!-- Bouton favori en haut à droite -->
                        <a href="{{ path('app_favori_add', {'formation_id': formation.id}) }}"
                        style="position: absolute; top: 10px; right: 10px; z-index: 1;">
                            <button class="btn btn-sm" 
                                    style="background-color: #ff4757; color: white; border-radius: 50%; width: 36px; height: 36px; padding: 0;"
                                    title="Ajouter aux favoris">
                                <i class="fas fa-heart"></i>
                            </button>
                        </a>

                        {% if formation.photo %}
                            <img src="{{ asset(formation.photo) }}" class="card-img-top" alt="Image Formation" 
                                 style="height: 200px; object-fit: cover; transition: transform 0.5s ease;">
                        {% else %}
                            <div class="d-flex align-items-center justify-content-center" 
                                 style="height: 200px; background: linear-gradient(135deg, #f5f7fa 0%, #dfe3e8 100%); color: #6c757d;">
                                <i class="fas fa-image fa-3x opacity-25"></i>
                            </div>
                        {% endif %}

                        <div class="card-body pb-0">
                            <h5 class="card-title mb-3" style="font-weight: 600; color: #2c3e50;">
                                {{ formation.titre }}
                            </h5>
                            
                            <ul class="list-unstyled mb-4">
                                <li class="mb-2">
                                    <i class="fas fa-calendar-day text-primary me-2"></i>
                                    <strong>Date :</strong> {{ formation.date ? formation.date|date('Y-m-d') : 'N/A' }}
                                </li>
                                <li class="mb-2">
                                    <i class="fas fa-language text-primary me-2"></i>
                                    <strong>Langue:</strong> {{ formation.langue }}
                                </li>
                                <li class="mb-2">
                                    <i class="fas fa-certificate text-primary me-2"></i>
                                    <strong>Certification:</strong> 
                                    {% if formation.certifie %}
                                        <span class="badge " style="color: white; background-color: rgb(38, 227, 38);">Oui</span>
                                    {% else %}
                                        <span class="badge" style="color: white; background-color: rgb(243, 227, 80);">Non</span>
                                    {% endif %}
                                </li>
                                <li class="mb-2">
                                    <i class="fas fa-tag text-primary me-2"></i>
                                    <strong>Type :</strong> 
                                    <span class="badge bg-info">{{ formation.type }}</span>
                                </li>
                            </ul>
                        </div>
                        
                        <div class="card-footer bg-transparent border-0 pt-0 pb-3 d-flex justify-content-between align-items-center">
                            <span class="text-muted small">
                                <i class="fas fa-users me-1"></i>
                                {{ formation.nbPlace }} places
                            </span>
                            <a href="{{ path('app_formation_show2', {'id_f': formation.idF}) }}" 
                               class="btn btn-sm" 
                               style="background-color: #4771cb; color: white; border-radius: 20px; padding: 5px 15px;">
                                Voir Détails <i class="fas fa-arrow-right ms-1"></i>
                            </a>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    {% else %}
        <div class="text-center py-5" style="background-color: #f8f9fa; border-radius: 10px;">
            <i class="fas fa-book-open fa-3x mb-3" style="color: #6c757d;"></i>
            <h4 style="color: #6c757d;">Aucune formation trouvée</h4>
            <p class="text-muted">Essayez avec d'autres termes de recherche</p>
        </div>
    {% endif %}

    <div class="text-center mt-5 mb-4">
        <a href="{{ path('app_home') }}" class="btn" 
           style="background-color: #33ccc5; color: white; padding: 10px 25px; border-radius: 30px; text-decoration: none; box-shadow: 0 3px 10px rgba(51, 204, 197, 0.3);">
            <i class="fas fa-home me-2"></i>Retour à l'accueil
        </a>
    </div>
</div>

<style>
    .card:hover {
        transform: translateY(-8px);
        box-shadow: 0 12px 25px rgba(0, 0, 0, 0.15);
    }
    .card:hover img {
        transform: scale(1.05);
    }
    .badge {
        font-weight: 500;
        padding: 4px 8px;
        font-size: 0.8em;
    }
    .btn:hover {
        transform: translateY(-2px);
        box-shadow: 0 4px 8px rgba(0,0,0,0.1);
    }
    .list-unstyled li {
        padding: 3px 0;
    }
</style>
{% endblock %}
