<form method="post" action="{{ path('app_formation_delete', {'id_f': formation.id_f}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ formation.id_f) }}">
    <button class="btn">Delete</button>
</form>
