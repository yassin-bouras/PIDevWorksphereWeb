<header class="header-area header-sticky wow slideInDown" data-wow-duration="0.75s" data-wow-delay="0s">
    <div class="container">
        <div class="row">
            <div class="col-12">
                <nav class="main-nav">
                    <!-- ***** Logo Start ***** -->
                    <a href="{{ path('app_home') }}" class="logo">
                        <h4>Worksphere <img src="{{ asset('front/images/logo-icon.png') }}" alt=""></h4>
                    </a>
                    <!-- ***** Logo End ***** -->
                    <!-- ***** Menu Start ***** -->
                    <ul class="nav">

                        <li class="scroll-to-section"><a href="{{ path('app_home') }}">Home</a></li>
                        <li class="scroll-to-section"><a href="{{ path('app_formation_index2') }}">Formations</a></li>
                        <li class="scroll-to-section"><a href="{{ path('app_evennement_index') }}">Evénements</a></li>
                        <li class="scroll-to-section"><a href="{{ path('app_offre_front_index') }}">Offre d'Emploi</a></li>
                        <li class="scroll-to-section"><a href="{{ path('app_candidature_index') }}">Mes Candidatures</a>
                        </li>
                        
                        <li class="scroll-to-section dashboard-button">
                            <a href="/dashboardcandidat">
                                <span class="dash-icon"><i class="fas fa-tachometer-alt"></i></span>
                                <span class="dash-text">DASHBOARD</span>
                            </a>
                        </li>
                        

                        <li class="scroll-to-section">
                            <div class="main-blue-button">
                                <a href="/login">
                                    <i class="fas fa-sign-in-alt mr-1"></i> Se connecter
                                </a>
                            </div>
                        </li>

                    </ul>
                    <a class="menu-trigger">
                        <span>Menu</span>
                    </a>
                    <!-- ***** Menu End ***** -->
                </nav>
            </div>
        </div>
    </div>
</header>
<!-- Add this to your style section -->
<style>
    /* New Dashboard Button Style - Ultra-Minimized Version */
    .dashboard-button {
        margin-left: 5px; /* Reduced margin */
    }

    .dashboard-button a {
        background: linear-gradient(135deg, #33ccc5 0%, #2a9df4 100%) !important;
        color: white !important;
        padding: 4px 8px !important; /* Further reduced padding */
        border-radius: 20px !important; /* Even smaller radius */
        font-weight: 600 !important;
        letter-spacing: 0.2px; /* Minimal letter spacing */
        box-shadow: 0 2px 6px rgba(51, 204, 197, 0.25); /* Lighter shadow */
        border: none !important;
        display: flex !important;
        align-items: center !important;
        transition: all 0.3s ease !important;
        text-transform: uppercase;
        font-size: 0.65rem; /* Even smaller font */
    }

    .dashboard-button a:hover {
        transform: translateY(-1px); /* Minimal hover effect */
        box-shadow: 0 3px 8px rgba(51, 204, 197, 0.4);
        background: linear-gradient(135deg, #2bb3ae 0%, #1a8aff 100%) !important;
    }

    .dashboard-button a:active {
        transform: translateY(1px);
    }

    .dash-icon {
        background: rgba(255, 255, 255, 0.2);
        width: 16px; /* Tiny icon container */
        height: 16px; /* Tiny icon container */
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        margin-right: 4px; /* Minimal spacing */
        font-size: 0.6rem; /* Tiny icon */
    }

    .dash-text {
        position: relative;
        font-size: 0.7rem; /* Ensure text is small too */
    }

    .dash-text::after {
        content: '';
        position: absolute;
        width: 100%;
        height: 1px; /* Thinnest underline */
        bottom: -1px; /* Very close to text */
        left: 0;
        background-color: rgba(255, 255, 255, 0.6);
        transform: scaleX(0);
        transition: transform 0.3s ease;
    }

    .dashboard-button a:hover .dash-text::after {
        transform: scaleX(1);
    }

    /* Responsive adjustments */
    @media (max-width: 991px) {
        .dashboard-button a {
            padding: 3px 6px !important;
            font-size: 0.6rem;
        }

        .dash-icon {
            width: 14px;
            height: 14px;
            font-size: 0.55rem;
            margin-right: 2px;
        }
    }
</style>
<!-- ***** Header Area End ***** -->