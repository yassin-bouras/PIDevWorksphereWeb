
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=0">
    <title>Dleohr - Login</title>
    <link rel="shortcut icon" href="{{ asset('back/img/favicon.png') }}">
    <link rel="stylesheet" href="{{ asset('back/css/bootstrap.min.css') }}">
    <link rel="stylesheet" href="{{ asset('back/plugins/fontawesome/css/fontawesome.min.css') }}">
    <link rel="stylesheet" href="{{ asset('back/plugins/fontawesome/css/all.min.css') }}">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css">
    <style>
        :root {
            --primary: #4a9fff;
            --primary-light: #e6f3ff;
            --primary-dark: #2b7cd3;
            --secondary: #64b5f6;
            --accent: #90caf9;
            --light: #f8f9fa;
            --dark: #546e7a;
            --danger: #ef5350;
            --success: #66bb6a;
            --warning: #ffca28;
            --info: #29b6f6;
            --light-blue: #bbdefb;
            --blue-grey: #eceff1;
        }
        
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #e3f2fd, #bbdefb);
            height: 100vh;
            margin: 0;
            overflow-x: hidden;
        }
        
        .particles-container {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 0;
            pointer-events: none;
        }
        
        .particle {
            position: absolute;
            background: rgba(255, 255, 255, 0.5);
            border-radius: 50%;
            animation: float 15s infinite ease-in-out;
        }
        
        @keyframes float {
            0%, 100% {
                transform: translate(0, 0) rotate(0deg) scale(1);
                opacity: 0;
            }
            25% {
                opacity: 0.5;
            }
            50% {
                transform: translate(100px, -100px) rotate(180deg) scale(1.5);
                opacity: 0.2;
            }
            75% {
                opacity: 0.5;
            }
        }
        
        .login-container {
            display: flex;
            min-height: 100vh;
            align-items: center;
            justify-content: center;
            position: relative;
            z-index: 1;
            padding: 20px;
        }
        
        .login-card {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            box-shadow: 0 10px 25px rgba(75, 159, 255, 0.15);
            overflow: hidden;
            width: 100%;
            max-width: 1000px;
            display: flex;
            min-height: 600px;
        }
        
        .login-sidebar {
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            color: white;
            padding: 40px;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            width: 40%;
            position: relative;
            overflow: hidden;
        }
        
        .login-sidebar::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: linear-gradient(45deg, transparent, rgba(255, 255, 255, 0.1), transparent);
            transform: rotate(45deg);
            animation: shine 8s infinite;
        }
        
        @keyframes shine {
            0% {
                top: -50%;
                left: -50%;
            }
            100% {
                top: 150%;
                left: 150%;
            }
        }
        
        .sidebar-content {
            position: relative;
            z-index: 2;
        }
        
        .login-logo {
            width: 100%;
            max-width: 180px;
            margin-bottom: 30px;
            transition: transform 0.3s;
        }
        
        .login-logo:hover {
            transform: scale(1.05);
        }
        
        .sidebar-text {
            margin-bottom: 20px;
            line-height: 1.6;
            font-size: 16px;
        }
        
        .login-form {
            padding: 40px;
            width: 60%;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }
        
        .form-title {
            font-size: 28px;
            font-weight: 600;
            margin-bottom: 10px;
            color: var(--dark);
        }
        
        .form-subtitle {
            color: #78909c;
            margin-bottom: 30px;
        }
        
        .input-group {
            position: relative;
            margin-bottom: 25px;
        }
        
        .input-group label {
            position: absolute;
            top: 15px;
            left: 15px;
            color: #90a4ae;
            transition: all 0.3s;
            pointer-events: none;
        }
        
        .form-input {
            width: 100%;
            padding: 15px;
            border: 2px solid #e3f2fd;
            border-radius: 10px;
            background: white;
            font-size: 16px;
            transition: all 0.3s;
        }
        
        .form-input:focus {
            border-color: var(--primary);
            box-shadow: 0 0 0 3px rgba(74, 159, 255, 0.2);
            outline: none;
        }
        
        .form-input:focus + label,
        .form-input:not(:placeholder-shown) + label {
            top: -10px;
            left: 10px;
            font-size: 12px;
            background: white;
            padding: 0 5px;
            color: var(--primary);
        }
        
        .toggle-password {
            position: absolute;
            right: 15px;
            top: 15px;
            cursor: pointer;
            color: #90a4ae;
            transition: color 0.3s;
        }
        
        .toggle-password:hover {
            color: var(--primary);
        }
        
        .remember-forgot {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 25px;
        }
        
        .custom-checkbox {
            display: flex;
            align-items: center;
        }
        
        .custom-checkbox input {
            width: 18px;
            height: 18px;
            margin-right: 8px;
        }
        
        .forgot-links {
            display: flex;
            flex-direction: column;
            align-items: flex-end;
        }
        
        .forgot-link {
            color: var(--primary);
            text-decoration: none;
            font-size: 14px;
            margin-bottom: 8px;
            transition: color 0.3s;
        }
        
        .forgot-link:hover {
            color: var(--primary-dark);
            text-decoration: underline;
        }
        
        .login-btn {
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            color: white;
            border: none;
            border-radius: 10px;
            padding: 15px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s;
            margin-bottom: 20px;
            box-shadow: 0 5px 15px rgba(74, 159, 255, 0.3);
        }
        
        .login-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 20px rgba(74, 159, 255, 0.4);
        }
        
        .login-btn:active {
            transform: translateY(0);
        }
        
        .login-btn:disabled {
            background: #cfd8dc;
            cursor: not-allowed;
            box-shadow: none;
        }
        
        .social-login {
            display: flex;
            flex-direction: column;
            gap: 15px;
            margin-top: 20px;
        }
        
        .social-btn {
            display: flex;
            align-items: center;
            justify-content: center;
            border-radius: 10px;
            padding: 12px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s;
            text-decoration: none;
        }
        
        .social-btn i {
            margin-right: 10px;
            font-size: 20px;
        }
        
        .google-btn {
            background: white;
            color: #DB4437;
            border: 2px solid #DB4437;
        }
        
        .google-btn:hover {
            background: #DB4437;
            color: white;
        }
        
        .face-btn {
            background: white;
            color: var(--info);
            border: 2px solid var(--info);
        }
        
        .face-btn:hover {
            background: var(--info);
            color: white;
        }
        
        .alert {
            padding: 15px;
            border-radius: 10px;
            margin-bottom: 20px;
            animation: fadeIn 0.5s;
        }
        
        .alert-danger {
            background-color: #ffebee;
            border-left: 4px solid var(--danger);
            color: #c62828;
        }
        
        .alert-success {
            background-color: #e8f5e9;
            border-left: 4px solid var(--success);
            color: #2e7d32;
        }
        
        /* Modal styles */
        .modal-custom {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.5);
            display: none;
            align-items: center;
            justify-content: center;
            z-index: 1050;
            backdrop-filter: blur(5px);
            animation: fadeIn 0.3s;
        }
        
        .modal-content-custom {
            background: white;
            padding: 30px;
            border-radius: 20px;
            width: 90%;
            max-width: 500px;
            text-align: center;
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.2);
            transform: scale(0.8);
            animation: zoomIn 0.3s forwards;
        }
        
        @keyframes zoomIn {
            from {
                transform: scale(0.8);
                opacity: 0;
            }
            to {
                transform: scale(1);
                opacity: 1;
            }
        }
        
        @keyframes fadeIn {
            from {
                opacity: 0;
            }
            to {
                opacity: 1;
            }
        }
        
        .modal-content-custom h5 {
            font-size: 24px;
            margin-bottom: 15px;
            color: var(--dark);
        }
        
        .modal-content-custom p {
            margin-bottom: 20px;
            color: #78909c;
        }
        
        .modal-content-custom textarea {
            width: 100%;
            height: 120px;
            margin: 15px 0;
            padding: 15px;
            border-radius: 10px;
            border: 2px solid #e3f2fd;
            resize: none;
            font-family: inherit;
            transition: all 0.3s;
        }
        
        .modal-content-custom textarea:focus {
            border-color: var(--primary);
            box-shadow: 0 0 0 3px rgba(74, 159, 255, 0.2);
            outline: none;
        }
        
        .modal-buttons {
            display: flex;
            justify-content: center;
            gap: 15px;
            margin-top: 20px;
        }
        
        .modal-btn {
            padding: 12px 25px;
            border-radius: 10px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s;
            border: none;
        }
        
        .modal-btn-success {
            background: var(--success);
            color: white;
        }
        
        .modal-btn-success:hover {
            background: #43a047;
            transform: translateY(-2px);
        }
        
        .modal-btn-danger {
            background: var(--danger);
            color: white;
        }
        
        .modal-btn-danger:hover {
            background: #e53935;
            transform: translateY(-2px);
        }
        
        /* Face auth modal */
        #faceAuthModal .modal-content-custom {
            max-width: 600px;
        }
        
        #video, #canvas {
            width: 100%;
            max-width: 480px;
            border-radius: 15px;
            margin: 0 auto 20px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        }
        
        #video {
            display: block;
        }
        
        #canvas {
            display: none;
        }
        
        .spinner {
            display: inline-block;
            width: 80px;
            height: 80px;
            border: 5px solid rgba(144, 202, 249, 0.3);
            border-radius: 50%;
            border-top-color: var(--primary);
            animation: spin 1s linear infinite;
            margin: 20px auto;
            display: none;
        }
        
        @keyframes spin {
            to {
                transform: rotate(360deg);
            }
        }
        
        .status-text {
            color: var(--primary);
            font-weight: 600;
            margin: 15px 0;
            min-height: 24px;
        }
        
        /* Responsive adjustments */
        @media (max-width: 992px) {
            .login-card {
                flex-direction: column;
                height: auto;
            }
            
            .login-sidebar, .login-form {
                width: 100%;
            }
            
            .login-sidebar {
                padding: 30px;
            }
        }
        
        @media (max-width: 576px) {
            .login-form, .login-sidebar {
                padding: 20px;
            }
            
            .form-title {
                font-size: 24px;
            }
            
            .social-login {
                flex-direction: column;
            }
            
            .remember-forgot {
                flex-direction: column;
                align-items: flex-start;
                gap: 15px;
            }
            
            .forgot-links {
                align-items: flex-start;
            }
        }
    </style>
</head>
<body>
    <!-- Animated background particles -->
    <div class="particles-container" id="particles-container"></div>
    
    <div class="login-container">
        <div class="login-card animate__animated animate__fadeIn">
            <!-- Left sidebar with logo and info -->
            <div class="login-sidebar">
                <div class="sidebar-content animate__animated animate__fadeInLeft">
                    <img class="login-logo" src="{{ asset('Images/logo.png') }}" alt="Dleohr Logo">
                    <p class="sidebar-text">Log in to access your personalized dashboard and start managing your HR operations.</p>
                </div>
            </div>
            
            <!-- Right side login form -->
            <div class="login-form animate__animated animate__fadeInRight">
                <div id="alerts-container"></div>
                
                {% if is_banned %}
                <div class="alert alert-danger">
                    <i class="fas fa-exclamation-circle"></i> Too many failed login attempts. Please try again after {{ ban_until|date('Y-m-d H:i:s') }}.
                </div>
                {% endif %}
                
                {% if hcaptcha_error %}
                <div class="alert alert-danger">
                    <i class="fas fa-exclamation-circle"></i> {{ hcaptcha_error }}
                </div>
                {% endif %}
                
                <h2 class="form-title">Login</h2>
                <p class="form-subtitle">Access to our dashboard</p>
                
                <form id="loginForm" method="post">
                    <div class="input-group">
                        <input type="email" class="form-input" name="email" id="email" placeholder=" " required autofocus>
                        <label for="email">Email Address</label>
                    </div>
                    
                    <div class="input-group">
                        <input type="password" class="form-input" name="password" id="password" placeholder=" " required>
                        <label for="password">Password</label>
                        <span class="fas fa-eye toggle-password"></span>
                    </div>
                    
                    <div class="h-captcha" data-sitekey="{{ hcaptcha_site_key }}"></div>
                    
                    <div class="remember-forgot">
                        <div class="custom-checkbox">
                            <input type="checkbox" id="remember_me" name="_remember_me">
                            <label for="remember_me">Remember me</label>
                        </div>
                        
                        <div class="forgot-links">
                            <a class="forgot-link" href="/user/new">Don't have an account?</a>
                            <a class="forgot-link" href="/forgot">Forgot password?</a>
                        </div>
                    </div>
                    
                    <button class="login-btn animate__animated" type="submit" id="loginButton" 
                            {% if is_banned %}disabled{% endif %}>
                        <i class="fas fa-sign-in-alt"></i> Login
                    </button>
                    
                    <div class="social-login">
                        <a href="{{ path('connect_google') }}" class="social-btn google-btn">
                            <i class="fab fa-google"></i> Login with Google
                        </a>
                        
                        <a href="#" id="faceAuthLink" class="social-btn face-btn">
                            <i class="fas fa-camera"></i> Login with Face ID
                        </a>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <!-- Reclamation Modal -->
    <div id="reclamationModal" class="modal-custom">
        <div class="modal-content-custom">
            <h5><i class="fas fa-exclamation-triangle"></i> Account Restricted</h5>
            <p>Your account has been temporarily restricted. Please submit your reclamation message below:</p>
            <textarea id="reclamationMessage" placeholder="Enter your message..."></textarea>
            <div class="modal-buttons">
                <button id="submitReclamation" class="modal-btn modal-btn-success">
                    <i class="fas fa-paper-plane"></i> Submit
                </button>
                <button id="cancelReclamation" class="modal-btn modal-btn-danger">
                    <i class="fas fa-times"></i> Cancel
                </button>
            </div>
        </div>
    </div>

    <!-- Face Auth Modal -->
    <div id="faceAuthModal" class="modal-custom">
        <div class="modal-content-custom">
            <h5><i class="fas fa-camera"></i> Face Authentication</h5>
            <p>Position your face in front of the camera for identification.</p>
            <video id="video" autoplay></video>
            <canvas id="canvas"></canvas>
            <div class="spinner" id="faceAuthSpinner"></div>
            <p class="status-text" id="faceAuthStatus"></p>
            <div class="modal-buttons">
                <button id="cancelFaceAuth" class="modal-btn modal-btn-danger">
                    <i class="fas fa-times"></i> Cancel
                </button>
            </div>
        </div>
    </div>


    <script src="{{ asset('back/js/jquery-3.6.0.min.js') }}"></script>
    <script src="https://js.hcaptcha.com/1/api.js" async defer></script>
    <script>
        $(document).ready(function () {
            // Handle regular login form submission
            $('#loginForm').on('submit', function (e) {
                e.preventDefault();

                const email = $('input[name="email"]').val();
                const password = $('input[name="password"]').val();
                const hcaptchaResponse = $('[name="h-captcha-response"]').val();
                const data = {
                    email: email,
                    password: password,
                    'h-captcha-response': hcaptchaResponse
                };

                $.ajax({
                    url: '/login',
                    method: 'POST',
                    contentType: 'application/json',
                    data: JSON.stringify(data),
                    success: function (response) {
                        if (response.token) {
                            handleSuccessfulLogin(response.token);
                        }
                    },
                    error: function (xhr) {
                        const errorMessage = xhr.responseJSON?.error || 'Login failed';
                        $('.login-right-wrap').prepend(`<div class="alert alert-danger">${errorMessage}</div>`);
                        hcaptcha.reset(); // Reset hCaptcha on error
                    }
                });
            });

            // Handle password visibility toggle
            $('.toggle-password').click(function () {
                const input = $(this).siblings('.pass-input');
                input.attr('type', input.attr('type') === 'password' ? 'text' : 'password');
                $(this).toggleClass('fa-eye fa-eye-slash');
            });

            let stream = null;
            let faceCheckInterval = null;
            const video = document.getElementById('video');
            const canvas = document.getElementById('canvas');
            const cancelFaceAuth = document.getElementById('cancelFaceAuth');
            const faceAuthModal = $('#faceAuthModal');
            const CHECK_INTERVAL = 5000;

            $('#faceAuthLink').click(function (e) {
                e.preventDefault();
                faceAuthModal.fadeIn();
                startVideo();
            });

            function startVideo() {
                navigator.mediaDevices.getUserMedia({ video: true })
                    .then(function (mediaStream) {
                        stream = mediaStream;
                        video.srcObject = stream;
                        // Start periodic face checking
                        startFaceChecking();
                    })
                    .catch(function (err) {
                        stopFaceChecking();
                        faceAuthModal.fadeOut();
                        $('.login-right-wrap').prepend('<div class="alert alert-danger">Camera access denied or unavailable.</div>');
                    });
            }

            function startFaceChecking() {
                // Clear any existing interval
                stopFaceChecking();
                // Start checking every CHECK_INTERVAL milliseconds
                faceCheckInterval = setInterval(checkFace, CHECK_INTERVAL);
            }

            function stopFaceChecking() {
                if (faceCheckInterval) {
                    clearInterval(faceCheckInterval);
                    faceCheckInterval = null;
                }
            }

            function checkFace() {
                if (!stream || !faceAuthModal.is(':visible')) {
                    stopFaceChecking();
                    return;
                }

                canvas.width = video.videoWidth;
                canvas.height = video.videoHeight;
                canvas.getContext('2d').drawImage(video, 0, 0, canvas.width, canvas.height);
                const imageData = canvas.toDataURL('image/png');

                // Send the captured image to the face-auth endpoint
                $.ajax({
                    url: '{{ path('face_auth') }}',
                    method: 'POST',
                    contentType: 'application/json',
                    data: JSON.stringify({ image: imageData }),
                    success: function (response) {
                        if (response.token) {
                            stopFaceChecking();
                            stopVideoStream();
                            faceAuthModal.fadeOut();
                            handleSuccessfulLogin(response.token);
                        }
                    },
                    error: function (xhr) {
                        // Do not display error immediately; allow retries
                        // Optionally, limit retries or show error after a certain number of failures
                    }
                });
            }

            function stopVideoStream() {
                if (stream) {
                    stream.getTracks().forEach(track => track.stop());
                    stream = null;
                }
            }

            cancelFaceAuth.addEventListener('click', function () {
                stopFaceChecking();
                stopVideoStream();
                faceAuthModal.fadeOut();
            });

            // Ensure cleanup when modal is hidden (e.g., via external means)
            faceAuthModal.on('hidden', function () {
                stopFaceChecking();
                stopVideoStream();
            });

            // Handle successful login (for both password and face auth)
            function handleSuccessfulLogin(token) {
                document.cookie = `BEARER=${token}; Path=/; SameSite=Strict`;

                const payload = JSON.parse(atob(token.split('.')[1]));
                const userRole = payload.roles && payload.roles.length > 0 ? payload.roles[0].toLowerCase() : null;

                $.ajax({
                    url: '/jwt/user',
                    method: 'POST',
                    contentType: 'application/json',
                    data: JSON.stringify({ token: token }),
                    success: function (res) {
                        const user = res.user;

                        if (user.banned) {
                            if (user.reclamation) {
                                $('.login-right-wrap').prepend('<div class="alert alert-danger">You are banned. Reclamation already submitted.</div>');
                            } else {
                                $('#reclamationModal').fadeIn();

                                $('#submitReclamation').one('click', function () {
                                    const msg = $('#reclamationMessage').val().trim();
                                    if (msg) {
                                        $.ajax({
                                            url: "{{ path('app_lr_reclamation') }}",
                                            method: 'POST',
                                            contentType: 'application/json',
                                            data: JSON.stringify({
                                                email: user.email,
                                                message: msg
                                            }),
                                            success: function () {
                                                $('#reclamationModal').fadeOut();
                                                $('.login-right-wrap').prepend('<div class="alert alert-success">Reclamation submitted successfully.</div>');
                                            },
                                            error: function (xhr) {
                                                const err = xhr.responseJSON?.error || "Error saving reclamation.";
                                                $('.login-right-wrap').prepend(`<div class="alert alert-danger">${err}</div>`);
                                            }
                                        });
                                    } else {
                                        alert("Please write a message before submitting.");
                                    }
                                });

                                $('#cancelReclamation').one('click', function () {
                                    $('#reclamationModal').fadeOut();
                                });
                            }
                            return;
                        }

                        switch (userRole) {
                            case 'candidat':
                                window.location.href = '/dashboardcandidat';
                                break;
                            case 'rh':
                                window.location.href = '/dashboard';
                                break;
                            case 'employe':
                                window.location.href = '/dashboardemploye';
                                break;
                            case 'manager':
                                window.location.href = '/dashboardmanager';
                                break;
                            default:
                                $('.login-right-wrap').prepend('<div class="alert alert-danger">Unknown role</div>');
                        }
                    }
                });
            }
        });
    </script>
</body>
</html>