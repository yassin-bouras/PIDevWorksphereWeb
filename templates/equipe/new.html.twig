<!DOCTYPE html>

<title>New Equipe</title>

{% block body %}
    <h1>Create new Equipe</h1>

    {{ include('equipe/_form.html.twig') }}

    <a href="{{ path('app_equipe_index') }}">back to list</a>
{% endblock %}
