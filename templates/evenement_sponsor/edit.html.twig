<!DOCTYPE html>

<title>Edit EvenementSponsor</title>

{% block body %}
    <h1>Edit EvenementSponsor</h1>

    {{ include('evenement_sponsor/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_evenement_sponsor_index') }}">back to list</a>

    {{ include('evenement_sponsor/_delete_form.html.twig') }}
{% endblock %}
