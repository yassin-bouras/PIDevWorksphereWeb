<form method="post" action="{{ path('app_evennement_delete', {'idEvent': evennement.idEvent}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ evennement.idEvent) }}">
    <button class="btn">Delete</button>
</form>
