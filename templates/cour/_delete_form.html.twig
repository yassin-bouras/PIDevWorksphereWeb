<form method="post" action="{{ path('app_cour_delete', {'id_c': cour.IdC}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cour.IdC) }}">
    <button class="btn btn-danger">Supprimer  <i data-feather="trash-2"></i> </button>
</form>
