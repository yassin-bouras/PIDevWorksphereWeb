<!DOCTYPE html>

<title>Favori</title>

{% block body %}
    <h1>Favori</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id_favori</th>
                <td>{{ favori.idFavori }}</td>
            </tr>
            <tr>
                <th>Id_user</th>
                <td>{{ favori.idUser }}</td>
            </tr>
            <tr>
                <th>Id_f</th>
                <td>{{ favori.idF }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_favori_index') }}">back to list</a>

    <a href="{{ path('app_favori_edit', {'id_favori': favori.id_favori}) }}">edit</a>

    {{ include('favori/_delete_form.html.twig') }}
{% endblock %}
