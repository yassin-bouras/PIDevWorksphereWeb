{% extends '/dashboard/base2.html.twig' %}

{% block title %}Entretien Audit Trail{% endblock %}

{% block content %}
    <div class="audit-container" style="margin-top:-50%;">
    <!-- Header Section -->
    <div class="audit-header">
        <div class="header-content">
            <h1>
                <i class="fas fa-history"></i> historique des entrtiens 
            </h1>
            <p>Enregistrement complet de toutes les modifications apportées aux dossiers d'entretien</p>
        </div>
    </div>

    <!-- Filter Controls -->
    <div class="filter-controls">
        <div class="filter-group">
            <div class="filter-item">
                <label><i class="fas fa-filter"></i> Action Type</label>
                <div class="filter-buttons">
                    <button class="filter-btn active" data-filter="all">All</button>
                    <button class="filter-btn" data-filter="create">
                        <i class="fas fa-plus-circle"></i> Created
                    </button>
                    <button class="filter-btn" data-filter="update">
                        <i class="fas fa-pencil-alt"></i> Updated
                    </button>
                    <button class="filter-btn" data-filter="delete">
                        <i class="fas fa-trash-alt"></i> Deleted
                    </button>
                </div>
            </div>
            
            <div class="filter-item">
                <label><i class="fas fa-calendar-alt"></i> Date Range</label>
                <div class="date-range">
                    <input type="date" class="date-input" id="start-date">
                    <span>to</span>
                    <input type="date" class="date-input" id="end-date">
                </div>
            </div>
        </div>
        
        <div class="search-box">
            <i class="fas fa-search"></i>
            <input type="text" placeholder="Search records...">
            <button class="search-btn">Search</button>
        </div>
    </div>

    <!-- Audit Table -->
    <div class="audit-table-container">
        <table class="audit-table">
            <thead>
                <tr>
                    <th width="120px">Action</th>
                    <th width="180px">Field Changed</th>
                    <th>Previous Value</th>
                    <th>New Value</th>
                    <th width="160px">Modified At</th>
                    <th width="100px">Entretien ID</th>
                </tr>
            </thead>
            <tbody>
                {% for entry in history %}
                <tr class="audit-row" data-action="{{ entry.action|lower }}" data-date="{{ entry.date|date('Y-m-d') }}">
                    <td>
                        <span class="action-tag {{ entry.action|lower }}">
                            <i class="action-icon"></i>
                            {{ entry.action }}
                        </span>
                    </td>
                    <td>{{ entry.FieldChanged }}</td>
                    <td>
                        <div class="value-display old-value">
                            {{ entry.oldValue }}
                        </div>
                    </td>
                    <td>
                        <div class="value-display new-value">
                            {{ entry.newValue }}
                        </div>
                    </td>
                    <td>
                        <div class="timestamp">
                            <div class="date">{{ entry.date|date('Y-m-d') }}</div>
                            <div class="time">{{ entry.date|date('H:i:s') }}</div>
                        </div>
                    </td>
                    <td>
                        <span class="id-tag">#{{ entry.entretien.id }}</span>
                    </td>
                </tr>
                {% else %}
                <tr class="no-results">
                    <td colspan="6">
                        <div class="empty-state">
                            <i class="fas fa-database"></i>
                            <h4>No audit records found</h4>
                            <p>No changes have been recorded yet</p>
                        </div>
                    </td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>

    <!-- Pagination -->
    <div class="table-footer">
        <div class="results-count">
            Showing <span>{{ history|length }}</span> of {{ history|length }} records
        </div>
        <div class="pagination">
            <button disabled><i class="fas fa-chevron-left"></i></button>
            <span>Page 1 of 1</span>
            <button disabled><i class="fas fa-chevron-right"></i></button>
        </div>
    </div>
</div>

<style>
/* Base Styles */
:root {
    --primary: #4361ee;
    --primary-light: #eef2ff;
    --success: #10b981;
    --success-light: #ecfdf5;
    --warning: #f59e0b;
    --warning-light: #fffbeb;
    --danger: #ef4444;
    --danger-light: #fef2f2;
    --gray-100: #f3f4f6;
    --gray-200: #e5e7eb;
    --gray-300: #d1d5db;
    --gray-500: #6b7280;
    --gray-700: #374151;
    --gray-900: #111827;
    --shadow-sm: 0 1px 2px 0 rgba(0,0,0,0.05);
    --shadow-md: 0 4px 6px -1px rgba(0,0,0,0.1);
    --radius: 8px;
}

.audit-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 1.5rem;
    font-family: 'Segoe UI', system-ui, sans-serif;
}

/* Header Styles */
.audit-header {
    margin-bottom: 2rem;
    padding-bottom: 1rem;
    border-bottom: 1px solid var(--gray-200);
}

.audit-header h1 {
    font-size: 1.75rem;
    font-weight: 700;
    color: var(--gray-900);
    margin-bottom: 0.5rem;
    display: flex;
    align-items: center;
    gap: 0.75rem;
}

.audit-header h1 i {
    color: var(--primary);
}

.audit-header p {
    color: var(--gray-500);
    font-size: 1rem;
}

/* Filter Controls */
.filter-controls {
    background: white;
    border-radius: var(--radius);
    padding: 1.25rem;
    margin-bottom: 1.5rem;
    box-shadow: var(--shadow-sm);
    border: 1px solid var(--gray-200);
}

.filter-group {
    display: flex;
    gap: 2rem;
    margin-bottom: 1.5rem;
}

.filter-item {
    flex: 1;
}

.filter-item label {
    display: block;
    font-size: 0.875rem;
    font-weight: 500;
    color: var(--gray-700);
    margin-bottom: 0.75rem;
}

.filter-item label i {
    margin-right: 0.5rem;
    color: var(--gray-500);
}

.filter-buttons {
    display: flex;
    gap: 0.75rem;
}

.filter-btn {
    padding: 0.5rem 1rem;
    border-radius: 6px;
    border: 1px solid var(--gray-200);
    background: white;
    color: var(--gray-700);
    font-size: 0.875rem;
    font-weight: 500;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    cursor: pointer;
    transition: all 0.2s ease;
}

.filter-btn:hover {
    background: var(--gray-100);
    border-color: var(--gray-300);
}

.filter-btn.active {
    background: var(--primary-light);
    border-color: var(--primary);
    color: var(--primary);
}

.filter-btn i {
    font-size: 0.8rem;
}

.date-range {
    display: flex;
    align-items: center;
    gap: 0.75rem;
}

.date-input {
    padding: 0.5rem 0.75rem;
    border: 1px solid var(--gray-200);
    border-radius: 6px;
    font-size: 0.875rem;
    color: var(--gray-700);
}

.date-range span {
    color: var(--gray-500);
    font-size: 0.875rem;
}

.search-box {
    position: relative;
    max-width: 400px;
}

.search-box i {
    position: absolute;
    left: 1rem;
    top: 50%;
    transform: translateY(-50%);
    color: var(--gray-500);
}

.search-box input {
    width: 100%;
    padding: 0.75rem 1rem 0.75rem 2.5rem;
    border: 1px solid var(--gray-200);
    border-radius: var(--radius);
    font-size: 0.875rem;
    color: var(--gray-700);
    transition: all 0.2s ease;
}

.search-box input:focus {
    outline: none;
    border-color: var(--primary);
    box-shadow: 0 0 0 3px rgba(67, 97, 238, 0.1);
}

.search-btn {
    position: absolute;
    right: 0.25rem;
    top: 50%;
    transform: translateY(-50%);
    padding: 0.5rem 1rem;
    background: var(--primary);
    color: white;
    border: none;
    border-radius: 6px;
    font-size: 0.875rem;
    font-weight: 500;
    cursor: pointer;
    transition: all 0.2s ease;
}

.search-btn:hover {
    background: #3a56d4;
}

/* Table Styles */
.audit-table-container {
    background: white;
    border-radius: var(--radius);
    box-shadow: var(--shadow-sm);
    border: 1px solid var(--gray-200);
    overflow: hidden;
}

.audit-table {
    width: 80%;
    border-collapse: collapse;
}

.audit-table th {
    padding: 1rem 1.25rem;
    text-align: left;
    font-size: 0.75rem;
    font-weight: 600;
    color: var(--gray-500);
    text-transform: uppercase;
    letter-spacing: 0.5px;
    background: var(--gray-100);
    border-bottom: 1px solid var(--gray-200);
}

.audit-table td {
    padding: 1rem 1.25rem;
    font-size: 1rem;
    color: var(--gray-700);
    border-bottom: 1px solid var(--gray-200);
    vertical-align: top;
}

.audit-table tr:last-child td {
    border-bottom: none;
}

.audit-table tr:hover td {
    background: var(--gray-100);
}

/* Action Tags */
.action-tag {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.375rem 0.75rem;
    border-radius: 50px;
    font-size: 0.75rem;
    font-weight: 600;
}

.action-tag i {
    font-size: 0.7rem;
}

.create {
    background: var(--success-light);
    color: var(--success);
}

.update {
    background: var(--warning-light);
    color: var(--warning);
}

.delete {
    background: var(--danger-light);
    color: var(--danger);
}

/* Value Displays */
.value-display {
    padding: 0.5rem;
    border-radius: 4px;
    max-width: 300px;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
}

.old-value {
    background: var(--danger-light);
    color: var(--danger);
}

.new-value {
    background: var(--success-light);
    color: var(--success);
}

/* Timestamp */
.timestamp {
    display: flex;
    flex-direction: column;
}

.date {
    font-weight: 500;
    color: var(--gray-700);
}

.time {
    font-size: 0.75rem;
    color: var(--gray-500);
}

/* ID Tag */
.id-tag {
    display: inline-block;
    padding: 0.25rem 0.5rem;
    background: var(--gray-100);
    color: var(--gray-700);
    border-radius: 4px;
    font-family: monospace;
    font-size: 0.75rem;
    font-weight: 600;
}

/* Empty State */
.no-results td {
    padding: 3rem !important;
    text-align: center;
}

.empty-state {
    max-width: 300px;
    margin: 0 auto;
    text-align: center;
}

.empty-state i {
    font-size: 2.5rem;
    color: var(--gray-200);
    margin-bottom: 1rem;
}

.empty-state h4 {
    font-size: 1rem;
    font-weight: 600;
    color: var(--gray-700);
    margin-bottom: 0.5rem;
}

.empty-state p {
    font-size: 0.875rem;
    color: var(--gray-500);
}

/* Table Footer */
.table-footer {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-top: 1.5rem;
    padding: 1rem 0;
}

.results-count {
    font-size: 0.875rem;
    color: var(--gray-500);
}

.results-count span {
    font-weight: 600;
    color: var(--gray-700);
}

.pagination {
    display: flex;
    align-items: center;
    gap: 0.75rem;
}

.pagination button {
    width: 2rem;
    height: 2rem;
    border-radius: 6px;
    border: 1px solid var(--gray-200);
    background: white;
    color: var(--gray-500);
    cursor: pointer;
    transition: all 0.2s ease;
}

.pagination button:hover:not(:disabled) {
    background: var(--gray-100);
    border-color: var(--gray-300);
}

.pagination button:disabled {
    opacity: 0.5;
    cursor: not-allowed;
}

.pagination span {
    font-size: 0.875rem;
    color: var(--gray-500);
}

/* Responsive Adjustments */
@media (max-width: 1024px) {
    .filter-group {
        flex-direction: column;
        gap: 1rem;
    }
    
    .search-box {
        max-width: 100%;
    }
}

@media (max-width: 768px) {
    .audit-container {
        padding: 1rem;
    }
    
    .filter-buttons {
        flex-wrap: wrap;
    }
    
    .audit-table th, 
    .audit-table td {
        padding: 0.75rem;
    }
}
</style>

<script>
document.addEventListener('DOMContentLoaded', function() {
    // Filter functionality
    const filterButtons = document.querySelectorAll('.filter-btn');
    const auditRows = document.querySelectorAll('.audit-row');
    const startDateInput = document.getElementById('start-date');
    const endDateInput = document.getElementById('end-date');
    
    // Action filter
    filterButtons.forEach(button => {
        button.addEventListener('click', function() {
            // Remove active class from all buttons
            filterButtons.forEach(btn => btn.classList.remove('active'));
            
            // Add active class to clicked button
            this.classList.add('active');
            
            const filterValue = this.dataset.filter;
            filterTable(filterValue);
        });
    });
    
    // Date range filter
    startDateInput.addEventListener('change', filterByDate);
    endDateInput.addEventListener('change', filterByDate);
    
    function filterTable(actionFilter) {
        auditRows.forEach(row => {
            const rowAction = row.dataset.action;
            const actionMatch = actionFilter === 'all' || rowAction === actionFilter;
            
            if (actionMatch) {
                row.style.display = '';
            } else {
                row.style.display = 'none';
            }
        });
    }
    
    function filterByDate() {
        const startDate = startDateInput.value;
        const endDate = endDateInput.value;
        
        auditRows.forEach(row => {
            const rowDate = row.dataset.date;
            const dateMatch = (!startDate || rowDate >= startDate) && 
                             (!endDate || rowDate <= endDate);
            
            if (dateMatch) {
                row.style.display = '';
            } else {
                row.style.display = 'none';
            }
        });
    }
    
    // Add animation to rows
    auditRows.forEach((row, index) => {
        row.style.opacity = '0';
        row.style.transform = 'translateY(10px)';
        row.style.transition = `opacity 0.3s ease ${index * 0.05}s, transform 0.3s ease ${index * 0.05}s`;
        
        setTimeout(() => {
            row.style.opacity = '1';
            row.style.transform = 'translateY(0)';
        }, 50);
    });
});
</script>
{% endblock %}